import java.util.EmptyStackException;/** * A class of stacks whose entries are stored in a chain of nodes. *  * @author Frank M. Carrano * @author Timothy M. Henry * @version 5.0 */public final class LinkedStack<T> implements StackInterface<T> {	private Node topNode; // References the first node in the chain	public LinkedStack() {		topNode = null;	} 	public void push(T newEntry) {		Node newNode = new Node(newEntry, topNode);		topNode = newNode;		//    topNode = new Node(newEntry, topNode); // Alternate code	} 	public T peek() {		if (isEmpty())			throw new EmptyStackException();		else			return topNode.getData();	} // end peek	public T pop() {		T top = peek(); // Might throw EmptyStackException		topNode = topNode.getNextNode();		return top;	} 	public boolean isEmpty() {		return topNode == null;	} 	public void clear() {		topNode = null; 	} 	@Override	public String toString() {		String s = "";		Node current = topNode;		while (current != null) {			s = current.data + " " + s;			current = current.next;		}		return s;	}	public boolean priorityPush(T element) {		Node currentNode = topNode;		Node lastNode = null;		while (currentNode != null) {			if (currentNode.data.equals(element)) {				if (lastNode == null) // first element is a match					return true;				else { 					lastNode.next = currentNode.next; // remove from current position					currentNode.next = topNode; // move to the top					topNode = currentNode; // reset topNode					return true;				}			}			lastNode = currentNode;			currentNode = currentNode.next;		}		Node newNode = new Node(element);		newNode.next = topNode;		topNode = newNode;		return false;	}	public T peekNext() {		Node top = topNode;		if (top == null) 			throw new EmptyStackException();		else if (top.next == null)			return null;		else			return top.next.data;	}		private class Node {		private T data; 		private Node next; 		private Node(T dataPortion) {			this(dataPortion, null);		} 		private Node(T dataPortion, Node linkPortion) {			data = dataPortion;			next = linkPortion;		}		private T getData() {			return data;		}		private void setData(T newData) {			data = newData;		}		private Node getNextNode() {			return next;		} 		private void setNextNode(Node nextNode) {			next = nextNode;		} 	} } 